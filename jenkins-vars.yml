---

# jenkins variables
project_name: docker-azahar
external_type: na
custom_version_command: |
  curl -s 'https://aur.archlinux.org/rpc/v5/info?arg[]=azahar' | jq -r '.results[] | select(.Name == "azahar") | .Version'
release_type: stable
release_tag: latest
ls_branch: master
repo_vars:
  - BUILD_VERSION_ARG = 'AZAHAR_VERSION'
  - LS_USER = 'linuxserver'
  - LS_REPO = 'docker-azahar'
  - CONTAINER_NAME = 'azahar'
  - DOCKERHUB_IMAGE = 'linuxserver/azahar'
  - DEV_DOCKERHUB_IMAGE = 'lsiodev/azahar'
  - PR_DOCKERHUB_IMAGE = 'lspipepr/azahar'
  - DIST_IMAGE = 'arch'
  - MULTIARCH = 'false'
  - CI = 'true'
  - CI_WEB = 'true'
  - CI_PORT = '3001'
  - CI_SSL = 'true'
  - CI_DELAY = '120'
  - CI_DOCKERENV = 'TZ=US/Pacific'
  - CI_AUTH = 'user:password'
  - CI_WEBPATH = ''
